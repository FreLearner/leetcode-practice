这道题很明显是滑动窗口
我用的方法是，维护一个窗口，在对比重复的时候，是将下一个要插入的和当前窗口内的所有元素进行一个对比，如果重复，则左边移动到第一个不重复的位置。
更好的方法是，大循环，每次修改左边指针向右移动，并且从Hashset中移除之前的窗口最左边，表示该元素开始的最长不重复子串，如果右指针的元素和冲口内的元素不同，则加入hashset
直到找到一个重复元素，此时不添加进窗口，比较此时窗口内元素的数量，如果数量大于max,则更新。
由于出现了重复元素，所以每次大循环将左边往右移动一个后都会将下一个待添加元素和hashset比较，直到大循环移出第重复的元素。