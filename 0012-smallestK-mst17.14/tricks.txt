这道题很容易想到堆
但是不要直接全部输入堆，因为每个数据进入堆都要比较调整，会增加时间开销
更好的方式是维护大小为k的最大堆但里面的值是最小的k个，每次和堆顶比较，如果比堆顶小，则弹出堆顶，插入元素。
这样相对全部插入堆，会减少一些调整不必要的插入开销。
另一种思路是可以用快速排序，因为是任意次序，快速排序每次能找到一个元素的最终位置。如果这个位置=k则左边全部添加进res返回,
如果大于k，则从左边进行下一次快速排序，
如果小于k，左边只是前k小的一部分，还需要从右边进行快速排序。
注意快排选择基准的时候，要随机选择，防止输入的队列基本有序退化成了o(n^2)。